{
  "name": "amarket",
  "version": "1.0.0",
  "description": "AniMarket - E-commerce Platform",
  "scripts": {
    "build": "cd ecommerce-app && npm install && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm start"
  }
}
